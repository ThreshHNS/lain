# lain

Fast MVP monorepo:
- `lain-scene` - Babylon.js scene for GitHub Pages
- `lain-app` - Expo app shell with WebView

`lain-scene` modes:
- `http://localhost:4173/?mode=awp` - original AWP scene
- `http://localhost:4173/?mode=slasher` - redirects to POC-based slasher scene
